# ─────────────────────────────────────────────────────────────────────────────
# Dépendances de NOTRE app à installer sur le Pi (carte CLONE) :
#     pip install -r requirements.txt
#
# ⚠️ À installer sur le CLONE de la carte de prod, jamais sur la carte de prod
#    elle-même (l'install peut upgrader numpy et perturber l'env de mesure).
# ─────────────────────────────────────────────────────────────────────────────

# --- Serveur / API (superviseur Flask) ---
flask>=3.0
flask-socketio>=5.3
simple-websocket>=1.0      # transport WebSocket du serveur de dev (l'app mobile
                           # force transports:['websocket']) -> requis pour le mobile

# --- IA : chargement + scoring du bundle Isolation Forest (fxs_iso.joblib) ---
numpy>=1.24
joblib>=1.3
scikit-learn>=1.6          # DE PRÉFÉRENCE 1.8.0 = version d'entraînement du bundle.
                           # Une autre 1.x charge quand même le modèle (avertissement
                           # bénin). Si l'IA ne se charge pas, l'app tourne sans :
                           # le dashboard affiche « IA indisponible », la mesure reste OK.

# ─────────────────────────────────────────────────────────────────────────────
# DÉJÀ PRÉSENTES sur la carte de prod (utilisées par les scripts d'origine) —
# NE PAS réinstaller/upgrader ici. Listées seulement pour référence si jamais
# elles manquaient sur une carte neuve :
#   adafruit-circuitpython-ads1x15     # -> import adafruit_ads1x15 (ADC ADS1115)
#   adafruit-blinka                    # -> import board, busio, digitalio
#   (raspi-gpio = utilitaire système, pas un paquet pip)
# ─────────────────────────────────────────────────────────────────────────────
